{
    "name": "pixelbrackets/give-notice",
    "description": "Send a request to a preset URI and don’t expect a response",
    "type": "project",
    "license": "GPL-2.0-or-later",
    "require": {
        "guzzlehttp/guzzle": "^7.0"
    },
    "autoload": {
        "psr-4": {"Pixelbrackets\\GiveNotice\\": "src/"}
    },
    "require-dev": {
        "phpunit/phpunit": "^8.0"
    },
    "scripts": {
        "test": [
            "phpunit tests/unit/"
        ]
    }
}
